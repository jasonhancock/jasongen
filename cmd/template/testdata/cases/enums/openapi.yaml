components:
  schemas:
    Foo:
      type: object
      required:
        - sort
      properties:
        sort:
          $ref: '#/components/schemas/GetUsersSortFieldEnum'
    GetUsersSortFieldEnum:
      type: string
      enum:
        - "created_at"
        - "username"
      title: "GetUsersSortFieldEnum"
      description: "Sort fields for get users"
