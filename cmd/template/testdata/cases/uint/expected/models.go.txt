// Code generated by template.test. DO NOT EDIT.
// template.test 1.2.3

package widgets

import (
	"fmt"
	"net/http"

	"github.com/jasonhancock/jasongen/params"
)

// Widget
type Widget struct {
	P1 uint8  `json:"p1"`
	P2 uint16 `json:"p2"`
	P3 uint32 `json:"p3"`
	P4 uint64 `json:"p4"`
}

// WidgetsListParams Parameters for WidgetsList
type WidgetsListParams struct {
	Qp1 *uint8
	Qp2 *uint16
	Qp3 *uint32
	Qp4 *uint64
}

// WidgetsListResponse
type WidgetsListResponse struct {
	Items []Widget `json:"items"`
}

func getWidgetsListParams(r *http.Request) (WidgetsListParams, error) {
	var p WidgetsListParams

	{ // qp1

		val, err := params.QueryParamUint8(
			r.URL.Query(),
			`qp1`,
			params.Required(false),
		)
		if err != nil {
			// TODO: need to continue processing other fields instead of aborting here.
			return p, err
		}
		p.Qp1 = val
	}

	{ // qp2

		val, err := params.QueryParamUint16(
			r.URL.Query(),
			`qp2`,
			params.Required(false),
		)
		if err != nil {
			// TODO: need to continue processing other fields instead of aborting here.
			return p, err
		}
		p.Qp2 = val
	}

	{ // qp3

		val, err := params.QueryParamUint32(
			r.URL.Query(),
			`qp3`,
			params.Required(false),
		)
		if err != nil {
			// TODO: need to continue processing other fields instead of aborting here.
			return p, err
		}
		p.Qp3 = val
	}

	{ // qp4

		val, err := params.QueryParamUint64(
			r.URL.Query(),
			`qp4`,
			params.Required(false),
		)
		if err != nil {
			// TODO: need to continue processing other fields instead of aborting here.
			return p, err
		}
		p.Qp4 = val
	}

	return p, nil
}

func (p WidgetsListParams) get() []string {
	var data []string

	if p.Qp1 != nil {
		data = append(data, "qp1", fmt.Sprintf("%d", *p.Qp1))
	}

	if p.Qp2 != nil {
		data = append(data, "qp2", fmt.Sprintf("%d", *p.Qp2))
	}

	if p.Qp3 != nil {
		data = append(data, "qp3", fmt.Sprintf("%d", *p.Qp3))
	}

	if p.Qp4 != nil {
		data = append(data, "qp4", fmt.Sprintf("%d", *p.Qp4))
	}

	return data
}
